# Screenshots - ภาพหน้าจอแอปพลิเคชัน

เนื่องจากเป็นโปรเจกต์ตัวอย่าง ภาพหน้าจอจะถูกเพิ่มเมื่อทำการ build และทดสอบแอปพลิเคชัน

## 📱 หน้าจอที่ควรมี Screenshots:

### 1. หน้าเข้าสู่ระบบ (Login Screen)
- ไฟล์: `login_screen.png`
- แสดง: ฟอร์มเข้าสู่ระบบพร้อมพื้นหลัง gradient

### 2. หน้าหลัก (Home Screen)
- ไฟล์: `home_screen.png`
- แสดง: เมนูนำทางและส่วนต่าง ๆ ของแอป

### 3. หน้าสินค้า (Product Screen)
- ไฟล์: `product_screen.png`
- แสดง: แบนเนอร์โปรโมชั่นเลื่อนอัตโนมัติและรายการสินค้า

### 4. หน้าตะกร้า (Cart Screen)
- ไฟล์: `cart_screen.png`
- แสดง: รายการสินค้าในตะกร้าและปุ่มชำระเงิน

### 5. หน้าโปรไฟล์ (Profile Screen)
- ไฟล์: `profile_screen.png`
- แสดง: ข้อมูลผู้ใช้และการตั้งค่า

## 📋 วิธีการถ่าย Screenshots:

### สำหรับ Android Emulator:
```bash
flutter run
# กด Ctrl+S ใน emulator หรือใช้ adb
adb exec-out screencap -p > screenshots/screen.png
```

### สำหรับ iOS Simulator:
```bash
flutter run
# ใช้ Command+S ใน simulator
xcrun simctl io booted screenshot screenshots/screen.png
```

### สำหรับ Web:
```bash
flutter run -d chrome
# ใช้ Developer Tools ถ่ายภาพหน้าจอ
```

## 🎨 ข้อควรระวังในการถ่าย Screenshots:
- ใช้ข้อมูลทดสอบที่สวยงาม
- ตรวจสอบให้แน่ใจว่า UI แสดงผลถูกต้อง
- ใช้อุปกรณ์จำลองที่มีหน้าจอสวย
- บันทึกในรูปแบบ PNG เพื่อความคมชัด

---

💡 **หมาย เหตุ**: Screenshots จะถูกเพิ่มเมื่อมีการทดสอบแอปพลิเคชันจริง